# 航迹鬼影问题情况描述与初步分析

## 现象描述

在地图上显示某条航迹并打开对应点迹后，可以看到绿色点迹沿航迹分布，但紫色航迹线除了连接主要点迹序列外，还出现了极细的斜向连线。该连线颜色与当前航迹线一致，看起来像同一条航迹额外连接了两个点。

从截图观察，这类线条端点仍落在点迹附近，因此初步判断它不是独立图层或随机像素噪声，而是航迹 polyline 渲染结果的一部分。

## 当前航迹绘制链路

ADS-B CSV 后端解析逻辑位于 `src-tauri/src/adsb.rs`：

- 以 `ICAO Address` 聚合点迹：`src-tauri/src/adsb.rs:52`
- 每条航迹内部按 timestamp 字符串排序：`src-tauri/src/adsb.rs:85`

前端转换和合并逻辑：

- DTO 转前端 `Track` 后按时间戳排序：`src/composables/convertTrack.ts:89`
- 从数据库恢复的 Track 也按时间戳排序：`src/composables/convertTrack.ts:127`
- 已存在同 key 航迹再次加入时，会按 `id + source` 合并新点并排序：`src/composables/useTracks.ts:60`

Cesium 绘制逻辑位于 `src/components/CesiumMap.vue`：

- `toCartesianArray()` 将 `TrackPoint[]` 转为 Cesium 坐标数组：`src/components/CesiumMap.vue:447`
- Polyline 通过 `CallbackProperty` 读取该坐标数组：`src/components/CesiumMap.vue:543`
- 点迹通过独立 billboard entity 绘制：`src/components/CesiumMap.vue:1225`

因此，正常情况下紫色线条是按 `Track.positions` 的数组顺序逐点连接，不存在额外的“随机连线”绘制代码。

## 已排除或降低优先级的方向

### CSV 字段映射整体错误

当前 CSV 字段映射与项目文档一致：

- `fields[0]` -> ICAO
- `fields[1]` -> latitude
- `fields[2]` -> longitude
- `fields[3]` -> heading
- `fields[4]` -> altitude
- `fields[5]` -> ground speed
- `fields[10]` -> timestamp
- `fields[15]` -> vertical rate

目前没有证据表明经纬度字段被反读、列偏移或整体解析错误。

### 跨多个 CSV 文件合并

用户确认实际只导入根目录下同一个 CSV 文件。虽然当前前端确实存在按 `id + source` 合并航迹的行为，但这不是本次截图现象的首要解释。

### 单纯时间断档直连

曾检查到单个 CSV 内存在同一 ICAO 在较长时间后再次出现的情况，这会导致长距离直连。但用户进一步说明截图中的异常线端点是时间和空间最近邻，不符合“跨长时间断档直连”的典型表现。因此该方向不能解释当前截图中的细斜线。

## 当前更可能的问题方向

### 1. Cesium 宽 polyline 的退化线段渲染伪影

Cesium 的 polyline 不是简单绘制 1 像素线，而是生成有宽度的三角形网格。若输入点序列中存在连续重复点、近重复点或极短线段，宽线三角化可能产生细长三角形或斜向拉丝，视觉上表现为“不属于航迹本身的连线”。

该方向与截图更吻合：

- 线条颜色与航迹一致
- 线条看起来很细
- 端点仍与当前航迹点附近相关
- 用户判断端点是时间和空间最近邻，说明不是明显的数据跳点

已做的显示层防护：

- 增加 `isFinitePoint()`，过滤非法经纬度/高度：`src/components/CesiumMap.vue:434`
- `toCartesianArray()` 在线条渲染前跳过连续近重复顶点：`src/components/CesiumMap.vue:447`

注意：该处理只影响紫色线条网格，不删除原始点迹，绿色点仍按原始 observations 显示。

### 2. 点迹 entity 与航迹线 entity 不同步

点迹和线条是两套 Cesium entity：

- 线条随 `props.tracks` 更新时会更新 `trailRef.positions`
- 点迹此前只在不存在时创建，如果同一 track key 的点数组变化，已有点迹不会必然重建

这可能造成视觉错配：绿色点看起来是旧数组或旧过滤状态，紫线则来自新数组。

已做的同步修正：

- `syncGlobalPointDots()` 中，当点数量变化时重建点迹
- 当点数量不变但位置变化时，逐个更新 billboard position
- 相关入口：`src/components/CesiumMap.vue:1304`

## 后续验证建议

1. 重新运行应用，复现同一条航迹并打开点迹，观察细斜线是否消失。
2. 若仍存在，记录该航迹的 `ICAO` 和截图位置，导出该航迹附近的连续点序列，确认异常线两端对应的 `positions[i]` / `positions[i+1]`。
3. 对比线条输入点数量与点迹数量，确认是否仍存在 entity 不同步。
4. 如确认仍是 Cesium 宽线问题，可进一步尝试将异常航迹的 polyline 改为更小宽度、禁用 geodesic 插值，或按小段拆分为多个 polyline entity 观察差异。

## 当前结论

目前不应把该现象定性为 ADS-B CSV 数据错误，也不应简单定性为 CSV 字段解析错误。

更合理的初步结论是：航迹点数据大体正确，异常线更可能出现在显示层，重点怀疑 Cesium 宽 polyline 对连续重复/近重复点的渲染伪影，以及点迹 billboard 与航迹 polyline 在更新过程中的不同步。
