# RadarView 项目逻辑审查问题总结

> 审查范围：前端 `src/`、后端 `src-tauri/src/`、导入 → Track 转换 → 合并/过滤/回放 → Cesium 渲染 → SQLite/settings 持久化全链路。
>
> 本文只整理较高把握、可能导致实际功能错误、状态不一致、数据丢失、渲染异常或持久化异常的问题。

---

## 总览

### 优先级定义

| 优先级 | 含义 |
|---|---|
| P0 / 严重 | 可能导致数据丢失、数据库不一致、用户操作结果消失、应用状态严重错误 |
| P1 / 高 | 可能导致显示状态错误、过滤/回放/设置失效、用户可明显感知的功能异常 |
| P2 / 中 | 边界条件、迁移、统计、健壮性问题，通常在异常数据或特殊状态下触发 |

### 建议修复顺序

1. **数据安全 / 持久化一致性**
   - 批次去重只按文件名
   - 后台保存失败无前端事件
   - `delete_batch()` 无事务
   - 删除航迹扣错 `track_count`
   - ADS-B 数值解析失败变 0
2. **前端状态一致性**
   - 管理面板 visible set 绕过过滤
   - 管理面板 batch 身份与地图 `id::source` 身份冲突
   - 设置加载失败后永久丢弃保存
   - replay speed 读取顺序错误
   - selection/isolation 恢复后被 `setAll()` 清空
3. **地图渲染 / 回放细节**
   - 回放线尾跳点
   - 点迹不随图层隐藏
   - 回放中途开启点迹显示未来点
   - 点迹自动色 UI 与地图不一致
4. **边界 / 健壮性**
   - `track_points` 同毫秒丢点
   - `ts_to_ms()` 格式校验不足
   - NaN/Inf 入库
   - backfill 静默吞错
   - 启动 `.expect()` 崩溃问题

---

# P0 / 严重问题

## 1. 批次去重只按 `file_name`，不同来源/不同路径同名文件会被误判为已导入

### 位置

- `src-tauri/src/lib.rs:44`
- `src-tauri/src/lib.rs:82`
- `src-tauri/src/lib.rs:114`
- `src-tauri/src/db.rs:608`

### 问题

ADS-B、Radar、RadarRaw 三类导入都调用：

```rust
db::batch_exists(&db_path_buf, &file_name)
```

而 `batch_exists()` 只按 `file_name` 查询：

```sql
SELECT COUNT(*) FROM batches WHERE file_name = ?1
```

没有区分：

- 数据源 `source`
- 文件完整路径
- 文件内容 hash
- 文件大小 / 修改时间

### 影响

例如以下文件会被误判为同一批次：

- `D:/data1/a.mat`
- `D:/data2/a.mat`
- ADS-B 的 `a.csv`
- Radar 的 `a.mat`

更严重的是：后端仍然会把解析结果返回给前端，所以地图上能看到数据；但后台不会保存到 SQLite。用户重启后，这批数据会消失。

这会造成：

- 当前地图状态与 SQLite 持久化状态不一致
- 批次面板缺记录
- 管理面板缺数据
- 用户误以为数据已经保存

### 建议修复

最低限度：

```sql
WHERE file_name = ?1 AND source = ?2
```

更稳妥的方案：

- `batches` 表增加：
  - `file_path`
  - `file_hash`
  - `file_size`
  - `modified_time`
- 建唯一索引：

```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_batches_unique_source_hash
ON batches(source, file_hash);
```

同时，去重检查最好放进 `save_batch()` 的 `BEGIN IMMEDIATE` 事务内部，避免并发重复导入竞态。

---

## 2. 后台保存失败时不发事件，前端可能永久显示“保存中”，且用户不知道数据没持久化

### 位置

- `src-tauri/src/lib.rs:49`
- `src-tauri/src/lib.rs:52`
- `src-tauri/src/lib.rs:87`
- `src-tauri/src/lib.rs:90`
- `src-tauri/src/lib.rs:119`
- `src-tauri/src/lib.rs:122`
- `src/composables/useTrackLoader.ts:73`
- `src/composables/useTrackLoader.ts:75`
- `src/App.vue:252`

### 问题

前端导入成功后设置 `persisting = true`，然后只依赖后端发出的 `batch-saved` 事件恢复状态。

但后端后台线程中：

```rust
if let Err(e) = db::save_batch(...) {
    eprintln!("background save failed: {}", e);
}
```

失败时只打印日志，不通知前端。

### 影响

会出现：

1. 地图上已经显示数据
2. SQLite 保存失败
3. UI 仍可能显示“保存中”
4. 用户重启后数据消失
5. 前端没有任何明确错误提示

这类问题很难被用户理解，容易被认为是“软件偶发丢数据”。

### 建议修复

后端无论成功失败都发事件，例如：

```ts
batch-save-finished {
  ok: boolean,
  file_name: string,
  source: string,
  error?: string
}
```

前端监听：

- `ok=true`：刷新批次列表，清除 `persisting`
- `ok=false`：清除 `persisting`，显示错误 toast，提示“当前数据未持久化”

---

## 3. 删除单条/批量航迹时不检查实际删除行数，可能把 `batch.track_count` 扣错甚至误删批次

### 位置

- `src-tauri/src/db.rs:1086`
- `src-tauri/src/db.rs:1093`
- `src-tauri/src/db.rs:1100`
- `src-tauri/src/db.rs:1142`
- `src-tauri/src/db.rs:1150`
- `src-tauri/src/db.rs:1158`

### 问题

删除 `saved_tracks` 后，不检查 DELETE 实际影响行数，直接执行：

```sql
UPDATE batches SET track_count = track_count - 1 WHERE id = ?1
```

批量删除里如果传入重复航迹，也可能重复扣减。

### 影响

如果前端传了：

- 已经删除的航迹
- 重复航迹
- 过期选择
- 被另一个操作并发删除的航迹

那么 `saved_tracks` 实际删除 0 行，但 `track_count` 仍减 1。

可能后果：

- `track_count` 变成负数
- `DELETE FROM batches WHERE track_count <= 0` 删除仍有航迹的批次
- `saved_tracks` 还在，但 `batches` 没了
- 管理面板 JOIN 查询查不到这些孤儿数据

### 建议修复

```rust
let affected = conn.execute(...)?;
if affected == 1 {
    // 再扣 track_count
}
```

更稳妥：删除后重算真实数量：

```sql
UPDATE batches
SET track_count = (
  SELECT COUNT(*) FROM saved_tracks WHERE batch_id = ?1
)
WHERE id = ?1;
```

批量删除前还应对 `(icao_address, batch_id)` 去重。

---

## 4. `delete_batch()` 多条 DELETE 不在事务里，失败会留下半删除状态

### 位置

- `src-tauri/src/db.rs:619`
- `src-tauri/src/db.rs:622`
- `src-tauri/src/db.rs:625`
- `src-tauri/src/db.rs:628`

### 问题

删除批次时依次执行：

```rust
DELETE FROM track_points WHERE batch_id = ?1
DELETE FROM saved_tracks WHERE batch_id = ?1
DELETE FROM batches WHERE id = ?1
```

但没有事务包裹。

### 影响

如果中间失败，会出现：

- `track_points` 删除了，但 `saved_tracks` 还在
- `saved_tracks` 删除了，但 `batches` 还在
- 时间过滤、统计、管理面板都可能不一致

### 建议修复

用 `BEGIN IMMEDIATE` 包裹整个删除流程，失败 rollback。

---

## 5. 管理面板按 batch 管，地图按 `id::source` 合并，多个批次同一目标会互相污染

### 位置

- `src/composables/useTrackManagement.ts:36`
- `src/composables/useTrackManagement.ts:297`
- `src/composables/useTrackManagement.ts:318`
- `src/composables/useTrackManagement.ts:335`
- `src/composables/useTracks.ts:11`
- `src/composables/useTracks.ts:48`

### 问题

管理面板内部可见集合是：

```ts
icao::batchId
```

但地图显示用的是：

```ts
id::source
```

同时 `useTracks.addTracks()` 也是按 `id::source` 合并。

### 影响

如果同一飞机 / 目标在多个批次里出现：

- 管理面板可以勾选某个 batch 的航迹
- 地图只知道 `icao::source`
- 多个 batch 的点会被合并成一个逻辑航迹
- 删除/隐藏某个 batch 时，地图可能仍残留被合并后的数据

本质是“管理面板看到的是批次维度，地图显示的是合并维度”的身份模型冲突。

### 建议修复

统一 Track 身份模型。推荐：

```ts
trackKey = `${id}::${source}::${batchId}`
```

需要：

- `Track` 类型增加 `batchId`
- Cesium entity id 使用完整 key
- selection / isolation / visible set 全部使用完整 key
- 管理面板和地图保持同一身份模型

如果业务确实需要跨批次合并，应明确 UI 上这是“合并视图”，并保留 batch 到 merged track 的映射。

---

# P1 / 高优先级问题

## 6. 管理面板可见集合绕过时间过滤和点数过滤

### 位置

- `src/App.vue:177`
- `src/App.vue:179`
- `src/App.vue:188`
- `src/composables/useTrackFilter.ts:79`

### 问题

`displayTracks` 当前逻辑是：

```ts
if (visibleTrackIds.value.size > 0) {
  return tracks.value.filter(...)
}

if (isolatedTrackId.value) {
  ...
}

return filteredTracks.value
```

只要管理面板有可见集合，就直接从原始 `tracks.value` 取数据，完全绕过 `filteredTracks.value`。

### 影响

用户开启时间过滤/点数过滤后，只要管理面板勾选了航迹，地图会显示未经筛选的完整航迹。

表现为：

- UI 显示筛选已激活
- 地图显示未筛选数据
- 回放也基于未筛选数据计算

### 建议修复

先算过滤基底：

```ts
const base = filteredTracks.value

if (visibleTrackIds.value.size > 0) {
  return base.filter(tr => visibleTrackIds.value.has(trackKey(tr.id, tr.source)))
}

if (isolatedTrackId.value) {
  return base.filter(tr => trackKey(tr.id, tr.source) === isolatedTrackId.value)
}

return base
```

---

## 7. 回放轨迹线在两个采样点之间不会连续刷新，线尾会跳点

### 位置

- `src/components/CesiumMap.vue:522-537` 附近

### 问题

`updateReplayPositions()` 里只有当：

```ts
trail.lo !== entities.lastTrailLo
```

时才更新 `trailRef.positions`。

但在两个原始采样点之间，`trail.lo` 不变，`trail.currentPoint` 会连续插值变化。

### 影响

回放时：

- 飞机 billboard 每帧移动
- 轨迹线尾端停在上一个采样点
- 直到进入下一个采样点才跳过去

用户会看到点在动、线不跟着动。

### 建议修复

回放中应至少每帧更新最后插值点。

可以保留优化，但判断条件要包括 currentPoint 变化。例如缓存上一次插值坐标：

```ts
if (
  trail.lo !== entities.lastTrailLo ||
  currentPointChanged(trail.currentPoint, entities.lastReplayPoint)
) {
  ...
}
```

---

## 8. 图层隐藏时，点迹实体不随 source 隐藏

### 位置

- `src/components/CesiumMap.vue:610-620`
- `src/components/CesiumMap.vue:1007-1027`
- `src/components/CesiumMap.vue:1086-1121`

### 问题

`visibility` watcher 只更新主航迹：

```ts
entities.polyline.show = vis
entities.billboard.show = vis
```

但点迹存在 `pointDotEntityMap` 里，没有同步图层可见性。

### 影响

用户关闭 Radar 图层：

- Radar 航迹线隐藏
- Radar 的点迹仍可能显示

这会导致图层控制失效。

### 建议修复

新增统一函数：

```ts
applyPointDotVisibility()
```

统一考虑：

- source 图层是否可见
- 是否回放中
- 当前 replayTime 对应可见点范围
- 手动/全局点迹状态

在以下位置调用：

- visibility watcher
- createPointDotsForTrack 后
- replayTime watcher
- syncGlobalPointDots 后

---

## 9. 回放中途开启点迹时，未来点会全部显示

### 位置

- `src/components/CesiumMap.vue:1007-1027`
- `src/components/CesiumMap.vue:558-568`
- `src/components/CesiumMap.vue:1039-1065`
- `src/components/CesiumMap.vue:1086-1121`

### 问题

`createPointDotsForTrack()` 创建点迹时默认所有 entity 都 `show=true`。

虽然回放下一帧会根据 `trail.lo` 修正，但如果用户在暂停状态开启点迹，就不会有下一帧修正。

### 影响

回放暂停在中途时，用户右键显示点迹：

- 未来点也立即全部显示
- 与航迹线回放进度不一致

### 建议修复

创建点迹时根据当前 `props.replayTime` 初始化 show：

```ts
show: props.replayTime === null || p.timestamp <= props.replayTime
```

同时叠加图层可见性。

---

## 10. 设置加载失败会导致后续所有设置保存永久丢弃

### 位置

- `src/composables/useSettingsPersistence.ts:57-74`
- `src/composables/useSettingsPersistence.ts:18-23`

### 问题

`scheduleSave()`：

```ts
if (!_loaded) return
```

但 `loadAllSettings()` 如果失败会 `return`，不会 `_loaded = true`。

### 影响

一旦启动时设置读取失败：

- 用户之后改线宽、颜色、点迹大小、筛选状态
- 全部被静默丢弃
- 本次会话所有设置保存失效

### 建议修复

```ts
export async function loadAllSettings() {
  try {
    _raw = await invoke(...)
    if (typeof _raw !== 'object' || _raw === null) _raw = {}
    await applySettings(_raw)
  } catch (e) {
    console.error(...)
    _raw = {}
  } finally {
    _loaded = true
  }
}
```

---

## 11. 回放速度持久化读取顺序错误，保存的速度不会生效

### 位置

- `src/App.vue:165-168`
- `src/App.vue:191`
- `src/App.vue:230`
- `src/composables/useSettingsPersistence.ts:56`
- `src/composables/useSettingsPersistence.ts:75`

### 问题

`getRawSetting('replay.speed')` 在 setup 阶段执行：

```ts
const savedSpeedRaw = getRawSetting('replay.speed')
```

但 `_raw` 要等 `onMounted -> loadAllSettings()` 后才有值。

### 影响

保存的 replay speed 实际永远读不到，`useReplay()` 使用默认值。

### 建议修复

在 `await loadAllSettings()` 后：

```ts
const raw = getRawSetting('replay.speed')
if (raw) replay.setSpeed(JSON.parse(raw))
```

或者把 `replay.speed` 纳入 `applySettings()`。

---

## 12. 启动时先恢复 selection/isolation，随后 `setAll()` 又清空，导致选择/隔离状态无法恢复

### 位置

- `src/composables/useSettingsPersistence.ts:171`
- `src/composables/useSettingsPersistence.ts:174`
- `src/composables/useSettingsPersistence.ts:177`
- `src/App.vue:230`
- `src/App.vue:237`
- `src/composables/useTracks.ts:130`

### 问题

`loadAllSettings()` 先恢复：

```ts
selectedId
isolatedTrackId
```

但随后 `App.vue` 加载持久化航迹并调用：

```ts
setAll(fromBackendTracks(saved))
```

`setAll()` 内部会清空选择/隔离。

### 影响

设置里保存了 selection/isolation，但启动后看不到恢复效果。

### 建议修复

方案一：

```ts
setAll(newTracks, { preserveSelection: true })
```

方案二：启动流程改成：

1. load settings raw
2. load tracks
3. validate selected/isolation key 是否存在
4. 再应用 selection/isolation

---

## 13. `get_distinct_options()` 在 source 为空时生成非法 SQL

### 位置

- `src-tauri/src/db.rs:1015`
- `src-tauri/src/db.rs:1018`
- `src-tauri/src/db.rs:1022`
- `src-tauri/src/db.rs:1036`

### 问题

当 source 为空：

```rust
source_clause = String::new()
```

后面 SQL 拼接：

```sql
FROM saved_tracks {}
AND json_extract(...)
```

会变成：

```sql
FROM saved_tracks
AND json_extract(...)
```

这是非法 SQL。

### 影响

管理面板获取“全部来源”的筛选选项时会失败。

### 建议修复

统一：

```sql
WHERE 1=1
```

再拼接：

```sql
AND ...
```

最好改成参数化查询。

---

## 14. ADS-B CSV 解析失败静默变成 0，会制造错误航迹点

### 位置

- `src-tauri/src/adsb.rs:31`
- `src-tauri/src/adsb.rs:42-47`

### 问题

多个字段：

```rust
parse().unwrap_or(0.0)
```

包括经纬度、高度、速度等。

### 影响

经纬度解析失败会变成：

```text
0, 0
```

轨迹会画到几内亚湾附近，并被持久化。

### 建议修复

- 使用 `csv` crate，不要手写 `split(',')`
- 经纬度解析失败应跳过该行或返回错误
- 校验范围：
  - lat `[-90, 90]`
  - lon `[-180, 180]`
  - timestamp 可解析

---

# P2 / 中优先级问题

## 15. `track_points` 唯一索引按 `(icao_address, batch_id, timestamp_ms)`，同毫秒多点会被静默丢弃

### 位置

- `src-tauri/src/db.rs:154`
- `src-tauri/src/db.rs:166`
- `src-tauri/src/db.rs:290`
- `src-tauri/src/db.rs:406`

### 问题

唯一索引：

```sql
ON track_points(icao_address, batch_id, timestamp_ms)
```

插入：

```sql
INSERT OR IGNORE
```

### 影响

同一目标同一毫秒如果有多个点，后面的点被静默丢弃。

`track_json` 里有完整点，`track_points` 表里少点，导致：

- 时间过滤结果不准
- 统计不准
- JSON 与展开点表不一致

### 建议修复

增加 `point_index`，唯一键改为：

```sql
(icao_address, batch_id, point_index)
```

或者至少不要静默 `IGNORE`，记录重复数量。

---

## 16. `ts_to_ms()` 未严格校验时间字符串，异常格式可能 panic 或解析成错误时间

### 位置

- `src-tauri/src/track.rs:93`
- `src-tauri/src/track.rs:98`
- `src-tauri/src/track.rs:102-112`

### 问题

只检查长度，然后直接：

```rust
b[0] - b'0'
```

没有校验数字字符和分隔符。

### 影响

异常时间字符串可能：

- debug 下 panic
- release 下解析成无意义日期
- 导致后台保存失败并回滚

### 建议修复

严格校验格式：

```text
YYYY-MM-DD HH:mm:ss(.SSS)
```

所有数字位置必须 `is_ascii_digit()`，分隔符必须匹配。

---

## 17. `save_batch()` 拼接浮点 SQL，没有处理 NaN / Inf

### 位置

- `src-tauri/src/db.rs:393`
- `src-tauri/src/db.rs:404-411`
- `src-tauri/src/adsb.rs:42-47`

### 问题

批量 INSERT 用 `format!()` 拼接浮点数。如果值是 `NaN` / `inf`，SQL 可能非法。

### 影响

前端当次能看到数据，但后台保存失败。重启后丢失。

### 建议修复

入库前：

```rust
if !pos.latitude.is_finite() { ... }
```

最好用 rusqlite 参数绑定；若保留拼接 SQL，必须过滤/规范化浮点值。

---

## 18. 全局时间范围计算忽略 timestamp 为 0 的合法值

### 位置

- `src/composables/useTrackFilter.ts:28-35`

### 问题

使用 truthy 判断：

```ts
if (t.minTimestamp && ...)
```

timestamp 为 `0` 会被跳过。

### 影响

Unix epoch 或相对时间戳数据中，`0` 是合法值。会导致全局时间范围错误。

### 建议修复

```ts
if (typeof t.minTimestamp === 'number' && Number.isFinite(t.minTimestamp)) {
  ...
}
```

---

## 19. `backfill_metadata()` / `backfill_track_points()` 静默吞掉行级错误

### 位置

- `src-tauri/src/db.rs:199`
- `src-tauri/src/db.rs:208`
- `src-tauri/src/db.rs:241`
- `src-tauri/src/db.rs:251`

### 问题

迁移时：

```rust
.filter_map(|r| r.ok())
```

错误行被静默跳过。

### 影响

如果历史数据有坏行：

- metadata 不完整
- track_points 少行
- 时间过滤/统计异常
- 用户和开发者都不知道迁移不完整

### 建议修复

不要吞掉错误。至少记录具体 batch/icao。更好是让迁移失败并保留错误信息。

---

## 20. 启动阶段多个 `.expect()` 会因资源/DB 问题直接崩溃

### 位置

- `src-tauri/src/lib.rs:287`
- `src-tauri/src/lib.rs:291`
- `src-tauri/src/lib.rs:293`
- `src-tauri/src/lib.rs:295`
- `src-tauri/src/lib.rs:300`

### 问题

启动资源路径、MBTiles、瓦片服务、app data dir、SQLite 初始化都用 `.expect()`。

### 影响

这些都是运行时环境依赖，失败时应用直接崩溃，用户只能看到打不开。

### 建议修复

`setup()` 返回 `Result`，应改成：

```rust
.map_err(|e| format!("...: {}", e))?
```

并把错误展示到前端或写入日志。

---

# 额外 UI / 状态一致性问题

## 21. 点迹颜色设置面板显示的“自动颜色”和地图实际自动颜色不一致

### 位置

- `src/components/panels/SettingsPanel.vue:207-209`
- `src/components/CesiumMap.vue:975-980`

### 问题

设置面板自动色显示固定：

```ts
return pointDotColors[src] ?? '#00ffcc'
```

但地图实际使用：

```ts
contrastColor(getEffectiveHex(source))
```

### 影响

用户看到设置面板是青色，但地图上可能是橙色、紫色或其他互补色。

如果用户打开颜色选择器再确认，可能把“自动色”意外变成固定 `#00ffcc`。

### 建议修复

把自动点迹颜色计算逻辑抽到共享 composable：

```ts
getAutoPointDotColor(source)
getEffectivePointDotColor(source)
```

SettingsPanel 和 CesiumMap 都使用同一个函数。

---

# 总结

当前最应该优先处理的是**数据不会丢、数据库不会不一致**这一类问题：

1. 批次去重只按文件名
2. 后台保存失败无事件
3. `delete_batch()` 无事务
4. 删除航迹扣错 `track_count`
5. ADS-B 数值解析失败变 0

这几项直接影响用户对软件可靠性的信任。前端渲染与回放问题虽然影响体验，但一般不会造成不可恢复的数据损失，可以放在第二阶段处理。
