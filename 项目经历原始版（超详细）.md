# RadarView — 雷达航迹可视化验证平台

> 项目完整技术文档 | 哈尔滨工业大学 电子信息专业 大三 | 2025年4月 — 2026年6月

## 一、项目背景

课题组与中国电子科技集团第二十二研究所合作雷达航迹形成项目。用 MATLAB 对雷达原始回波进行信号检测、点迹凝聚、航迹起始、滤波跟踪形成仿真航迹，需将仿真航迹与真实 ADS-B 数据在 3D 场景叠加对比以验证算法准确性。

痛点：MATLAB 只能 2D 展示，无法多数据源叠加、无时间轴回放、MAT/CSV 格式不统一。
成果：开发了 Tauri + Vue 3 + CesiumJS 桌面端可视化平台，已投入课题组日常使用，并衍生 Web 版本（radarview-web）。

## 二、量化指标

| 指标 | 数值 |
|------|------|
| 总代码量 | ~5200 行（前端 ~4000 行 + Rust ~1100 行） |
| 源文件数 | 30+ 个（21 前端 + 8 Rust） |
| 前端组件 | 6 个 Vue 组件 |
| Rust 函数 | 50+ 个 |
| IPC 命令 | 12 个 |
| 数据库表 | 4 张（batches / saved_tracks / track_points / tiles） |
| 数据库索引 | 8 个 |
| 前端打包体积 | 104KB |
| 可执行文件 | 17MB |
| 离线瓦片 | 1.1GB Natural Earth（0-8级） |
| CSV 测试数据 | 29MB / 27.8万行 / 2900条航迹 |
| MAT 测试数据 | 8.7MB |
| 回放速度 | 100x - 2000x（可自定义） |
| track_points 行数 | 28万+ 位置记录 |
| IPC 传输压缩比 | ~60%（TrackDto 短字段 + tuple 序列化） |
| 导入解析方式 | rayon 4核并行（两阶段 fold+reduce → par sort+assemble） |

## 三、技术架构

### 3.1 整体架构

三层架构：Vue 3 前端（CesiumJS 渲染层 + Composables 状态层）通过 Tauri IPC（JSON 序列化）与 Rust 后端（文件解析 / 数据库 / 内嵌 HTTP 瓦片服务）通信。

```
┌──────────────────────────────────────────────────────┐
│  Vue 3 前端 (TypeScript)                              │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │ CesiumJS    │  │ Composables  │  │ Components │  │
│  │ 3D渲染层    │  │ 状态管理层    │  │ UI组件层   │  │
│  └─────────────┘  └──────────────┘  └────────────┘  │
├──────────────────────────────────────────────────────┤
│  Tauri IPC (invoke / JSON 序列化)                     │
├──────────────────────────────────────────────────────┤
│  Rust 后端                                            │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │ 文件解析  │  │ SQLite   │  │ 内嵌HTTP瓦片服务   │  │
│  │ CSV/MAT  │  │ rusqlite │  │ tiny_http         │  │
│  └──────────┘  └──────────┘  └───────────────────┘  │
└──────────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 桌面框架 | Tauri 2 | 相比 Electron 体积小 20 倍，Rust 后端性能优异 |
| 前端 | Vue 3 Composition API + TypeScript | 组合式 API 天然适合逻辑复用 |
| 3D引擎 | CesiumJS 1.140 (WGS-84) | 业内唯一成熟的开源 3D 地球引擎 |
| 数据库 | SQLite (rusqlite) + WAL 模式 | 零配置嵌入式，WAL 支持大事务安全写入 |
| 瓦片服务 | tiny_http (Rust 内嵌 HTTP) | 轻量 HTTP 服务器，启动延迟 < 1ms |
| 文件对话框 | @tauri-apps/plugin-dialog | 原生系统对话框 |
| 外部工具 | convert_mat.exe (PyInstaller 打包) | MAT 文件转换 |
| 地图数据 | Natural Earth MBTiles (0-8级) | 全球离线底图 |

### 3.3 前端状态管理（Composables 单例模式）

不使用 Vuex/Pinia。每个 composable 在模块顶层声明 `ref()` / `reactive()` 实现跨组件共享——这是 Vue 3 模块系统天然提供的单例模式：

| Composable | 状态类型 | 职责 |
|------------|----------|------|
| useTracks | ref + computed | 航迹 CRUD、选择、隔离、按源分组 |
| useTrackFilter | ref + computed | 时间范围过滤 + 按源点数量过滤 |
| useReplay | ref | 回放引擎（播放/暂停/调速/二分插值） |
| useFlags | ref | 旗标标注系统（放置/重命名/删除/Vincenty测距） |
| useLayerVisibility | reactive | 图层显隐控制（per-source） |
| useLabelVisibility | ref | 航迹标签开关 |
| useLineWidth | reactive | 按数据源调节线宽（0.5-10px 范围滑块） |
| useTrackStyle | 纯函数 | 数据源到颜色/图标映射 |
| convertTrack | 纯函数 | Rust snake_case → TS camelCase 转换 |
| useGeoCalc | 纯函数 | 大地测量计算（Haversine / Vincenty / 方位角） |
| useTrackLoader | ref | 文件导入流程编排 + 分块转换进度 |

其中 `useLineWidth` 采用 `reactive<Record<DataSource, number>>()` 在模块顶层声明，通过 `watch(() => props.lineWidths, ..., { deep: true })` 实现 slider 拖动时实时更新 Cesium 折线宽度，无需重建实体。

## 四、核心功能详解

### 4.1 多数据源导入管线

**ADS-B CSV 导入流程（v2.0 性能优化版）**：

```
plugin-dialog 原生文件框
  → invoke('import_adsb_file', { filePath })
    → Rust: adsb::parse_adsb_csv()
      → 全量 read_to_string + 分行收集
      → Phase 1: rayon::par_iter().fold().reduce()
          ── 4 核并行解析 27.8 万行 CSV，每线程私有 BTreeMap 分组
      → Phase 2: rayon::into_par_iter().map()
          ── 多线程排序 positions + 组装 Track 结构
    → Track → TrackDto 转换（紧凑 IPC 格式）
      ── 短字段名（id/src/pts/lat/lng/hdg 等）
      ── 时间戳预计算为 epoch ms（字节算术解析，避免 chrono format-string 开销）
      ── PtDto 序列化为 JSON 数组 [ts,lat,lng,...] 而非对象 {ts:...,lat:...}
    → 返回 Vec<TrackDto> JSON（~12MB，比原 40MB 减少 70%）
  → 后台线程：std::thread::spawn → save_batch() 异步持久化
  → convertTrack.ts: fromTrackDto() 转换（时间戳已是 epoch ms，零解析开销）
  → 分块转换：每 300 条 await nextTick() 让出 UI 线程
  → useTracks().addTracks() 合并（去重 + 位置点补全）
  → CesiumMap watch 自动同步渲染
```

**性能对比（2900 tracks / 276K positions 实测）**：

| 阶段 | 优化前 | 优化后 | 手段 |
|------|--------|--------|------|
| CSV 解析 | ~2.0s | ~1.4s | rayon 4核并行（fold+reduce → map） |
| DTO 转换 | — | ~0.1s | 字节算术 ts_to_ms + into_par_iter |
| DB 持久化 | ~6-8s（阻塞） | ~0s（感知） | std::thread::spawn 后台异步 |
| IPC 传输 | ~2.5s（40MB JSON 对象） | ~1.5s（12MB JSON 数组） | 紧凑字段名 + tuple 序列化 |
| 前端转换 | ~1.2s（276K 次 parseTimestamp） | ~0.04s | Rust 预计算 epoch ms |

**CSV 19 列格式**：0=ICAO地址 1=纬度 2=经度 3=航向 4=高度(英尺) 5=地速 8=机型 9=注册号 10=时间戳 11=起飞机场 12=目的机场 13=航班号 15=垂直速率 16=ICAO航班号 18=航司

**雷达 MAT 导入流程**：

```
plugin-dialog 原生文件框
  → invoke('import_radar_file', { filePath })
    → Rust: radar::parse_mat_file()
      → find_converter() 三阶段搜索 convert_mat.exe
      → 启动子进程（--mode raw 为原始测量模式）
      → 120 秒超时保护（跨平台 kill）
      → Tauri Event 推送进度：converting(10%) → parsing(80%) → done(90%)
      → stdout JSON → serde 反序列化 → source 覆盖
    → db::save_batch() 持久化（同上）
  → 前端 convertInChunks() 分块转换 + 进度条
```

**导入加载动画**：三个导入按钮在 `loader.loading.value` 为 true 时禁用并显示 CSS 旋转动画，按钮文案从数据源名称切换为百分比进度。

### 4.2 数据库架构（核心）

#### 4.2.1 表设计

```sql
-- 批次元数据表
CREATE TABLE batches (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    file_name     TEXT NOT NULL,
    source        TEXT NOT NULL,       -- ADS-B / Radar / RadarRaw
    track_count   INTEGER NOT NULL,
    imported_at   TEXT NOT NULL        -- 导入时间
);

-- 航迹存储表（JSON Blob + 预计算元数据）
CREATE TABLE saved_tracks (
    icao_address  TEXT NOT NULL,
    batch_id      INTEGER NOT NULL,
    track_json    TEXT NOT NULL,       -- 整条航迹 JSON（含所有位置点）
    source        TEXT,               -- 冗余字段，加速过滤查询
    min_timestamp TEXT,               -- 该航迹最早时间点（索引覆盖）
    max_timestamp TEXT,               -- 该航迹最晚时间点（索引覆盖）
    point_count   INTEGER DEFAULT 0, -- 位置点数量
    PRIMARY KEY (icao_address, batch_id),
    FOREIGN KEY (batch_id) REFERENCES batches(id) ON DELETE CASCADE
);

-- 位置点表（行式存储，为窗口函数航迹匹配做准备）
CREATE TABLE track_points (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    icao_address  TEXT NOT NULL,
    batch_id      INTEGER NOT NULL,
    source        TEXT NOT NULL DEFAULT '',
    timestamp_ms  INTEGER NOT NULL,   -- epoch 毫秒（整数比较 O(1)）
    latitude      REAL NOT NULL,
    longitude     REAL NOT NULL,
    altitude      REAL DEFAULT 0,
    heading       REAL DEFAULT 0,
    ground_speed  REAL DEFAULT 0,
    vertical_rate REAL DEFAULT 0
);
```

#### 4.2.2 索引策略

```sql
-- saved_tracks 索引：加速时间范围和按源过滤
CREATE INDEX idx_tracks_source ON saved_tracks(source);
CREATE INDEX idx_tracks_time   ON saved_tracks(min_timestamp, max_timestamp);
CREATE INDEX idx_tracks_points ON saved_tracks(point_count);

-- track_points 索引：加速单航迹查询、时间范围扫描、按源过滤
CREATE UNIQUE INDEX idx_points_unique ON track_points(icao_address, batch_id, timestamp_ms);
CREATE INDEX        idx_points_track  ON track_points(icao_address, batch_id);
CREATE INDEX        idx_points_ts     ON track_points(timestamp_ms);
CREATE INDEX        idx_points_src    ON track_points(source);
```

#### 4.2.3 架构设计决策

| 决策 | 方案 | 理由 |
|------|------|------|
| 航迹存储 | JSON Blob 而非拆分位置点表 | 航迹总是整条被查询（增删改查/回放/过滤），避免多表 JOIN 和 N+1 问题 |
| 位置点表 | 独立 track_points 行式存储 | 为后续 SQL 窗口函数航迹匹配做准备（LAG/LEAD/PARTITION BY），同时支持时间-位置高效查询 |
| 冗余字段 | source / min_timestamp / max_timestamp / point_count | 用空间换时间，避免每次过滤都反序列化 JSON |
| 时间格式 | track_points.timestamp_ms 存 epoch 毫秒整数 | 整数比较直接走 CPU，比字符串比较快 10-100 倍 |
| WAL 模式 | PRAGMA journal_mode=WAL | 原生 delete 模式下 28 万行单事务 INSERT 会导致 B-tree 页损坏（实测"Rowid out of order"），WAL 模式安全支持大事务 |

#### 4.2.4 Schema 迁移策略（PRAGMA user_version）

传统方案每次启动用 `SELECT COUNT(*)` 或 `EXISTS` 子查询检测是否需要迁移，这在 28 万行表上开销显著。本方案采用 SQLite 内建 `PRAGMA user_version`：

```rust
// 启动时 O(1) 查询版本号
let version: i64 = conn
    .pragma_query_value(None, "user_version", |row| row.get(0))
    .unwrap_or(0);

if version < 1 {
    backfill_track_points(&conn)?;    // 仅首次执行
    // 建索引（backfill 后一次性构建，比逐行维护快 10 倍+）
    conn.execute_batch("CREATE UNIQUE INDEX ...");
    conn.pragma_update(None, "user_version", 1)?;
}
```

这保证了：(1) O(1) 启动检查 (2) 迁移只执行一次 (3) 后续版本只需递增版本号，可叠加多个迁移。

此外，对于 `saved_tracks` 的增量 schema 变更（新增列），采用 `has_column()` + `PRAGMA table_info` 的轻量检测方式，对已存在列跳过 ALTER TABLE，保证幂等性。

#### 4.2.5 历史数据回填策略（B-tree 损坏修复实录）⭐

**问题**：`track_points` 表新增后需要从 `saved_tracks.track_json` 回填 3211 条航迹的位置点到行式表。最初尝试用一条 SQL 解决：

```sql
-- ❌ 这条语句导致了 B-tree 页损坏
INSERT INTO track_points (...)
SELECT ...
FROM saved_tracks s, json_each(s.track_json, '$.positions') p
```

单条 INSERT 生成 28 万+ 行，SQLite 在 delete journal_mode 下无法安全处理此规模的事务，触发 `btreeInitPage() returns error code 11`（B-tree 内部页损坏），表现为"Rowid out of order"等难以排查的错误。

**根因分析**：
- delete journal_mode 在事务提交前将原始页复制到 rollback journal
- 单事务 INSERT 28 万行会触发大量 B-tree 页分裂和重平衡
- 页分裂过程中如果 journal 写入失败或内存不足，部分页的父子指针会断裂
- 表现为 `PRAGMA integrity_check` 报 `rowid X missing from index` 或 `btreeInitPage error`

**修复方案（三层防护）**：

```
第一层：启用 WAL 模式
  PRAGMA journal_mode=WAL;
  → 大事务不再依赖 rollback journal 的页级回滚
  → 写入先到 WAL 文件，checkpoint 时再合并到主 DB

第二层：Rust 侧分块处理
  rows.chunks(50)                    // 每次处理 50 条航迹为一个事务
    → BEGIN IMMEDIATE                // 显式事务，保证原子性
    → 每条航迹 JSON 反序列化
      → positions.chunks(500)        // 500 行一批 INSERT（控制 SQL 长度）
      → format! 宏生成 VALUES 字面量
      → INSERT OR IGNORE
    → COMMIT / ROLLBACK

第三层：INSERT OR IGNORE 幂等保护
  → 即使重复执行也不会产生重复数据
  → user_version 机制保证只执行一次
  → 索引在全部数据回填完成后一次性创建（比逐行维护快 10 倍+）
```

**关键代码结构**：

```rust
fn backfill_track_points(conn: &Connection) -> Result<(), String> {
    // 1. 一次性查询全部待回填航迹
    let rows: Vec<(String, i64, Option<String>, String)> = ...;

    // 2. 分块事务
    for chunk in rows.chunks(50) {
        conn.execute("BEGIN IMMEDIATE", [])?;
        let result = (|| -> Result<(), String> {
            for (icao, batch_id, source, json) in chunk {
                let track: Track = serde_json::from_str(json)?;
                // 3. 批量生成 VALUES 子句
                let mut pos_values: Vec<String> = Vec::new();
                for pos in &track.positions {
                    let ts_ms = ts_to_ms(&pos.timestamp)?;
                    pos_values.push(format!(
                        "('{}',{},{},'{}',{},{},{},{},{},{})",
                        icao.replace('\'', "''"), batch_id, ts_ms,
                        source_str.replace('\'', "''"),
                        pos.latitude, pos.longitude, pos.altitude,
                        pos.heading, pos.ground_speed, pos.vertical_rate,
                    ));
                }
                // 4. 500 行一批执行 INSERT
                for batch in pos_values.chunks(500) {
                    let sql = format!(
                        "INSERT OR IGNORE INTO track_points (...) VALUES {}",
                        batch.join(",")
                    );
                    conn.execute(&sql, [])?;
                }
            }
            Ok(())
        })();
        // 5. 每 50 条航迹提交一次事务
        match result {
            Ok(()) => conn.execute("COMMIT", [])?,
            Err(e) => { let _ = conn.execute("ROLLBACK", []); return Err(e); }
        }
    }
    Ok(())
}
```

**验证结果**：
- 回填完成后执行 `PRAGMA integrity_check` → 返回 `ok`
- 确认 280,179 行位置记录完整无误
- 后续启动直接命中 `user_version >= 1`，跳过回填，启动耗时 < 50ms

#### 4.2.6 导入时的双写策略（两阶段 + 异步持久化）⭐

`saved_tracks` 和 `track_points` 在同一事务中写入，但架构上做了三处关键优化：

**优化 1 — prepared statement 复用**：`saved_tracks` 的 INSERT 不再逐条 `conn.execute()`（每次都重新解析 SQL），改为一次 `conn.prepare()` + 循环 `stmt.execute()`，消除 2900 次 SQL 解析开销：

```rust
let mut stmt = conn.prepare(
    "INSERT OR REPLACE INTO saved_tracks (...) VALUES (?1,?2,?3,?4,?5,?6,?7)"
)?;
for track in tracks {
    let json = serde_json::to_string(track)?;
    let (min_ts, max_ts, pt_count) = extract_track_meta(track);
    stmt.execute(params![...])?;
}
```

**优化 2 — track_points 两阶段批量插入**：旧方案逐条 track 交替执行 saved_tracks + track_points INSERT（大量上下文切换）。新方案先收集全部 28 万行 VALUES 字面量到 `Vec<String>`（预分配 `with_capacity`），再以 5000 行/批统一写入（从 ~600 次 SQL 调用降至 ~56 次）：

```rust
// Phase 1: prepared stmt 写 saved_tracks + 收集全部 track_points 行
let total_positions: usize = tracks.iter().map(|t| t.positions.len()).sum();
let mut point_rows: Vec<String> = Vec::with_capacity(total_positions);
{
    let mut stmt = conn.prepare("INSERT OR REPLACE INTO saved_tracks ...")?;
    for track in tracks {
        stmt.execute(...)?;
        for pos in &track.positions {
            point_rows.push(format!("('{}',{},{},...)", ...));
        }
    }
} // stmt 析构释放 conn 借用

// Phase 2: 一次性批量写入全部 track_points（5000 行/批）
for chunk in point_rows.chunks(5000) {
    conn.execute(&format!("INSERT OR IGNORE INTO track_points (...) VALUES {}", chunk.join(",")), [])?;
}
```

**优化 3 — 异步持久化（核心架构改进）**：导入命令不再阻塞等待 DB 写入完成——文件解析后立即返回紧凑 TrackDto 格式数据给前端渲染，DB 持久化在 `std::thread::spawn` 后台线程中静默执行。完成后通过 Tauri 事件 `emit("batch-saved")` 通知前端刷新批次列表：

```rust
// lib.rs import command 核心流程
let tracks = adsb::parse_adsb_csv(&file_path)?;        // rayon 并行解析
// ... batch_exists 去重检查 ...
std::thread::spawn(move || {                            // 后台写库
    db::save_batch(&path, &fname, "ADS-B", &tracks_clone)?;
    handle.emit("batch-saved", ());
});
Ok(tracks.into_par_iter().map(|t| t.to_dto()).collect()) // 立即返回紧凑 DTO
```

线程安全保障：(1) WAL 模式 + `busy_timeout(30s)` 处理多连接并发写入；(2) `tracks.clone()` 在 spawn 前完成，数据所有权分离；(3) 文件已存在时跳过写入但依然发射 `batch-saved` 事件，确保前端 persisting UI 正常清除。

#### 4.2.7 数据库查询优化

```rust
// O(log N) 全局时间范围查询（走 idx_tracks_time 索引覆盖）
pub fn get_global_time_range(path: &PathBuf) -> Result<Option<(i64, i64)>, String> {
    // SELECT MIN(min_timestamp), MAX(max_timestamp) FROM saved_tracks
    // WHERE min_timestamp IS NOT NULL AND min_timestamp != ''
}

// 按时间和点数量筛选航迹（动态 SQL 拼接 + 参数化查询）
pub fn load_filtered_tracks(
    path: &PathBuf,
    min_time_ms: Option<i64>,
    max_time_ms: Option<i64>,
    source_filters: &[SourceFilter],  // 每个源独立的点数量范围
) -> Result<Vec<Track>, String> {
    // 拼接 WHERE 子句：
    //   max_timestamp >= ? AND min_timestamp <= ?
    //   AND ((source = ? AND point_count >= ? AND point_count <= ?) OR ...)
    // 所有条件走索引覆盖，O(log N)
}
```

**参数化查询的动态构建**：由于 rusqlite 不支持直接拼接 `Vec<&dyn ToSql>`，采用 String 占位符编号方式（`?1, ?2, ...`），运行时构建 `param_dyn: Vec<&dyn ToSql>` 切片传入 `query_map`。

#### 4.2.8 删除时的级联清理

```rust
pub fn delete_batch(path: &PathBuf, batch_id: i64) -> Result<(), String> {
    let conn = Connection::open(path)?;
    // 三表顺序删除（先删子表再删主表）
    conn.execute("DELETE FROM track_points WHERE batch_id = ?1", ...)?;
    conn.execute("DELETE FROM saved_tracks WHERE batch_id = ?1", ...)?;
    conn.execute("DELETE FROM batches WHERE id = ?1", ...)?;
    Ok(())
}
```

前端调用前弹出 `window.confirm()` 确认框（"确定从数据库中删除 xxx.mat？该操作不可撤销。"），删除过程中按钮切换为红色 CSS 旋转动画，并通过 `deletingBatchId` 状态禁用其他删除按钮防止并发操作。

#### 4.2.9 导入性能优化架构（串行 → 异步持久化 + 紧凑 IPC）⭐⭐

**问题背景**：v1.0 架构下，导入命令的流程是逐层串行的：CSV 解析 → DB 双写（saved_tracks + track_points）→ 全量 JSON 序列化返回（40MB）→ 前端时间戳解析 → Cesium 渲染 → 首批航迹可见。实测导入 2900 条航迹需耗时约 15 秒，且其中前 12 秒用户完全看不到任何反馈。

**核心洞察**：用户根本不需要 DB 持久化完成就能使用航迹——他们需要的是"尽快看到数据"。DB 持久化是系统可靠性需求，不是用户体验需求。解耦二者。

**优化后架构**：

```
┌──────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│ rayon 并行解析    │ →  │ 紧凑 TrackDto 返回 │ →  │ 前端渲染（~3s可见）│
│ (Phase 1+2)      │    │ (12MB JSON)       │    │                  │
└──────────────────┘    └──────────────────┘    └─────────────────┘
         │
         └→ [后台线程] save_batch()
               └→ emit("batch-saved") → 刷新批次列表
```

**紧凑 IPC 格式设计（TrackDto / PtDto）**：

导入命令不再返回完整的 `Vec<Track>`（字段名长的 Rust struct），而是返回 `Vec<TrackDto>`——一个为 IPC 传输专门设计的轻量 DTO：

| 原字段 | DTO 字段 | 说明 |
|--------|---------|------|
| icao_address | id | 短 8 字符 |
| source | src | 短 4 字符 |
| positions | pts | 短 3 字符 |
| latitude / longitude / altitude | lat / lng / alt | 短 3 字符 |
| heading / ground_speed / vertical_rate | hdg / gs / vr | 短 2-3 字符 |
| timestamp (String) | ts (i64) | epoch ms 整数，省去前端 276K 次 parseTimestamp |
| flight_no / aircraft_type / ... | flt / typ / ... | `#[serde(skip_serializing_if)]` 跳过空值 |

`PtDto` 更进一步的优化——序列化为 JSON **数组**而非对象：

```rust
impl Serialize for PtDto {
    fn serialize<S: serde::Serializer>(&self, s: S) -> Result<S::Ok, S::Error> {
        let mut t = s.serialize_tuple(7)?;
        t.serialize_element(&self.ts)?;    // [ts,
        t.serialize_element(&self.lat)?;   //  lat,
        t.serialize_element(&self.lng)?;   //  lng,
        t.serialize_element(&self.alt)?;   //  alt,
        t.serialize_element(&self.hdg)?;   //  hdg,
        t.serialize_element(&self.gs)?;    //  gs,
        t.serialize_element(&self.vr)?;    //  vr]
        t.end()
    }
}
```

JSON 输出从 `{"ts":1718436645123,"lat":30.5,"lng":120.2,"alt":35000,"hdg":180,"gs":450,"vr":0}` (70 字节) 变为 `[1718436645123,30.5,120.2,35000,180,450,0]` (42 字节)——单条位置再省 **40%**。276K 条位置 × 42 字节 ≈ **11.6MB**，仅为原 40MB 的 29%。

**快速时间戳解析（ts_to_ms）**：

DTO 转换中每个 position 的 timestamp string 需转为 epoch ms。初版使用 chrono 的 `NaiveDateTime::parse_from_str(fmt)` 做 format-string 解析——276K 次调用耗时达 1.34 秒。改为字节索引算术（等效 JS 的 charCode）+ chrono 构造器：

```rust
pub fn ts_to_ms(s: &str) -> Result<i64, String> {
    let b = s.as_bytes();
    let y  = (b[0]-b'0')*1000 + (b[1]-b'0')*100 + (b[2]-b'0')*10 + (b[3]-b'0');
    let mo = (b[5]-b'0')*10 + (b[6]-b'0');
    // ... 日、时、分、秒、毫秒同理 ...
    let date = NaiveDate::from_ymd_opt(y, mo, d)?;      // 构造器，无格式解析
    let time = NaiveTime::from_hms_milli_opt(h, mi, s, ms)?;
    Ok(NaiveDateTime::new(date, time).and_utc().timestamp_millis())
}
```

276K 次调用从 1.34s → **~50ms**，配合 `into_par_iter()` 多线程分发，总 DTO 转换 < 100ms。

**rayon 并行 CSV 解析**：

原 `adsb::parse_adsb_csv()` 逐行串行解析 CSV 并按 ICAO 分组。改为两阶段并行：

```
Phase 1: par_iter().fold().reduce()
  → 每个线程独立解析分得的行 → 各自 BTreeMap<ICAO, Vec<Position>>
  → reduce 阶段合并各线程的局部 Map

Phase 2: into_par_iter().map()
  → 对每个 ICAO 分组并行排序 positions + 组装 Track 结构
```

利用 4 核并行，CSV 解析从 ~2.0s 降至 ~1.4s。

**前端 UI 状态机（persisting 状态）**：

后台保存期间，导入按钮保持禁用并显示"保存中"+ CSS 旋转动画，防止用户重复点击导入：

```
按钮状态：ADS-B → spinner 45% → spinner 保存中 → ADS-B
            空闲        解析中       后台存库中       完成
```

`batch-saved` 事件触发 `loader.onPersistComplete()` 清除 persisting 状态并刷新批次列表。



### 4.3 3D 地图渲染（CesiumJS）

**初始化流程**：

```
invoke('get_tile_server_port') 获取瓦片端口
  → new Cesium.Viewer({
      scene3DOnly: true,            // 仅 3D 模式
      requestRenderMode: true,      // 按需渲染，空闲时不消耗 GPU
      maximumRenderTimeChange: Infinity, // 不限制渲染帧间隔
      skyBox: false,                // 关闭天空大气（离线场景不需要）
      skyAtmosphere: false,
      baseLayer: false,             // 不使用 Cesium 默认 Bing Maps 底图
    })
  → UrlTemplateImageryProvider('http://127.0.0.1:{port}/tiles/{z}/{x}/{y}.png')
    (0-8 级离线瓦片)
  → camera.setView(110°E, 25°N, 12000km)  初始视角覆盖中国全境
```

**航迹实体结构**：

每条航迹创建两个 Cesium Entity（复合 ID: `{trackId}::{source}`）：
- **Polyline**（`{id}::line`）：飞行路径折线，positions 为全部位置点的经纬高数组
- **Billboard**（`{id}::dot`）：当前位置点图标，初始置于最后一个位置点

**颜色方案**：

| 数据源 | 颜色 | 默认线宽 | 正常透明度 | 选中线宽 | 选中透明度 |
|--------|------|----------|------------|----------|------------|
| ADS-B | #00d4ff 青 | 2.0（可调） | 0.88 | 4.0 | 1.0 |
| Radar | #00ff88 绿 | 2.0（可调） | 0.88 | 4.0 | 1.0 |
| RadarRaw | #ff8800 橙 | 2.0（可调） | 0.75 | 4.0 | 1.0 |

**线宽动态调节**：

采用模块级 `reactive` 单例 + Vue 3 `watch` 深度监听方案，实现拖动滑块实时更新折线宽度：

```
App.vue slider drag
  → useLineWidth().setLineWidth(source, value)
    → lineWidths[source] 变化（reactive 代理触发响应式更新）
      → CesiumMap.vue watch(() => props.lineWidths, ..., { deep: true })
        → 遍历 entityMap，对所有非选中航迹更新:
          (entry.polyline.polyline as any).width = baseWidth(entry.source)
        → viewer.scene.requestRender() 请求单帧渲染
```

关键点：(1) 使用 `reactive` 而非 `ref`，使嵌套属性变更可被 `watch` 的 `{ deep: true }` 捕获；(2) 直接修改 `PolylineGraphics.width` 属性，无需销毁重建 Entity；(3) `Math.max(0.5, Math.min(10, width))` 约束范围保证视觉可用性；(4) 选中航迹始终保持 `SELECTED_WIDTH = 4.0`，不受滑块影响。

**同步策略（suspendEvents/resumeEvents 批量更新）**：

```typescript
function syncEntities(newTracks: Track[]) {
    viewer.entities.suspendEvents()    // 暂停 EntityCollection 事件广播
    // 批量增删改 entities...
    // 1. 移除不在 newTracks 中的实体
    // 2. 新增或更新存在的实体
    viewer.entities.resumeEvents()     // 恢复事件广播
    viewer.scene.requestRender()       // 单次请求渲染
}
```

这是 CesiumJS 官方推荐的高性能批量更新模式——避免逐个 entity 操作触发 N 次 `scene.requestRender()`，将 N 次渲染合并为 1 次。

**交互系统**：

| 交互 | 触发方式 | 效果 | 技术实现 |
|------|----------|------|----------|
| 悬停高亮 | MOUSE_MOVE → scene.pick() | 线宽 5.0 / 红色 #ff3333 / billboard 1.3x | 识别 entity ID 前缀，不覆盖已选中状态 |
| 点击选择 | LEFT_CLICK → pick entity | 隔离选中航迹 | emit('track-pick')，300ms delay 消除双击误触 |
| 双击置旗 | LEFT_DOUBLE_CLICK → pickEllipsoid | 放置旗标标注 | 禁用默认双击缩放，获取椭球面交点经纬度 |
| 双击删旗 | 双击在旗标 icon 上 | 删除旗标 | 识别 flag- 前缀的 entity ID |

**图层显隐**：`watch(visibility, ..., { deep: true })` 监听 per-source 布尔开关，遍历 entityMap 设置 `entity.show` 属性，无需增删实体。

**标签开关**：`watch(showLabels)` 遍历 entityMap 设置 `billboard.label.text` 和 `font`，显示时用 18px 大字，隐藏时用 12px 空串。

### 4.4 时序回放引擎

**核心算法**：

```
对每条航迹在 currentTime 时刻：
  positions 数组按时间戳严格递增（导入时 sort 保证）
  → 二分查找 O(log N) 定位 currentTime 落在哪两个位置点之间
  → 对所有 6 个字段（lat/lng/alt/heading/groundSpeed/verticalRate）线性插值
  → 更新 Billboard 的 ConstantPositionProperty
```

**驱动机制**：

```
requestAnimationFrame 循环：
  wallDelta = performance.now() - lastWallTime     // 真实时间流逝（毫秒）
  dataDelta  = wallDelta × speed                   // 转换为数据时间流逝
  currentTime += dataDelta
  到达 timeRange.end → 自动 pause()
```

**变速系统**：100x / 300x / 500x / 2000x 四档预设按钮 + 自定义输入框。航迹列表变更时 `watch(tracks)` 自动暂停并重置到起始时刻，避免回放位置错乱。

**CesiumMap 回放同步**：`watch(() => props.replayTime)` 调用 `findPositionAtTime()`（独立的二分查找+插值逻辑），更新全部航迹 billboard 位置。回放停止时所有 billboard 回到各航迹最后一个位置点。

### 4.5 时间窗口过滤

**两阶段过滤策略**：

```
阶段 1：元数据预过滤（O(N) 不遍历位置点）
  利用 saved_tracks 的 min_timestamp / max_timestamp 缓存值
  → 排除完全不与 [min, max] 窗口重叠的航迹
  → 显著减少阶段 2 的处理量

阶段 2：位置点级别过滤（仅对通过阶段 1 的航迹）
  positions.filter(p => p.timestamp ∈ [min, max])
  → 滤为 0 个点的航迹丢弃
  → 保留的航迹更新 positions + pointCount
```

**按源点数量过滤**：每个数据源可独立设置最少/最多点数阈值（如 RadarRaw 数据可能只需 > 50 个点的航迹），进一步筛除数据量不足的噪声航迹。`computed` 驱动，与时间过滤正交。

### 4.6 旗标标注与大地测量

**旗标 CRUD**：双击空白 → 自动编号"旗标N" → 点击标签可重命名 → × 按钮或双击图标删除 → checkbox 选 2 个用于测距。

**旗标编号复用**：删除旗标后，新创建的旗标会复用已释放的最小编号，避免编号无限增长。

**大地测量算法**：

| 算法 | 精度 | 适用场景 |
|------|------|----------|
| Haversine | ~0.3% 误差 | 球面近似，快速估算 |
| Vincenty | < 0.5mm 误差 | WGS-84 椭球面，100 次迭代收敛，对跖点兜底 |
| Initial Bearing | 大圆方位角 | 计算起止方位 |
| Bearing to Cardinal | 8 方位映射 | 显示"N/NE/E/SE/S/SW/W/NW" |

**大圆弧可视化**：`Cesium.EllipsoidGeodesic.interpolateUsingFraction()` 在 WGS-84 椭球面上计算 64 个等间距中间点，渲染黄色半透明折线，直观展示两旗标之间的最短路径。

### 4.7 离线瓦片服务器

```
tile_server.rs 核心流程:
  setup() 阶段
    → find_mbtiles() 扫描资源目录的 .mbtiles 文件
    → start_tile_server()
      → tiny_http::Server::http("127.0.0.1:0") 绑定（端口 0 = OS 自动分配）
      → std::thread::spawn 后台线程监听请求
      → 解析 URL: /tiles/{z}/{x}/{y}.png
      → XYZ → TMS 坐标转换：tms_y = (1<<z) - 1 - y
      → SQLite 查询: SELECT tile_data FROM tiles WHERE zoom_level=? AND tile_column=? AND tile_row=?
      → 返回 PNG bytes + CORS 头（Access-Control-Allow-Origin: *）
    → get_tile_server_port() 通过 AtomicU16 暴露端口给前端
```

已知优化项：每个请求都 `Connection::open()` SQLite（每次打开约 0.5ms），高频访问时可改为连接池复用或 `lazy_static` 全局连接。

## 五、Tauri IPC 接口清单

共 12 个命令（`lib.rs generate_handler!` 注册）：

| # | 命令 | 参数 | 返回 | 说明 |
|---|------|------|------|------|
| 1 | greet | name | String | 健康检查 |
| 2 | get_tile_server_port | - | u16 | 获取瓦片服务端口 |
| 3 | import_adsb_file | file_path | Result<Vec<TrackDto>> | ADS-B CSV 导入（rayon 并行解析 + 后台存库） |
| 4 | import_radar_file | file_path | Result<Vec<TrackDto>> | 雷达 MAT 导入（Python 转换 + 后台存库） |
| 5 | import_radar_raw_file | file_path | Result<Vec<TrackDto>> | 原始测量 MAT 导入 + 后台存库 |
| 6 | load_persisted_tracks | - | Result<Vec<Track>> | 加载全部已持久化航迹（完整 Track 格式） |
| 7 | get_batches_cmd | - | Result<Vec<BatchInfo>> | 获取批次列表 |
| 8 | load_batch_tracks_cmd | batch_id | Result<Vec<Track>> | 按批次加载航迹 |
| 9 | delete_batch_cmd | batch_id | Result<()> | 删除批次（三表级联） |
| 10 | get_time_range_cmd | - | Result<Option<TimeRange>> | O(1) 获取全局时间范围 |
| 11 | load_filtered_cmd | min_ms, max_ms, source_filters | Result<Vec<Track>> | 按时间和点数量筛选 |
| 12 | (Tauri Event) batch-saved | - | () | 后台存库完成通知 → 刷新批次列表 + 清除 persisting 状态 |

**关键设计**：导入命令（3-5）返回紧凑 `TrackDto` 格式（~12MB），DB 查询命令（6/8/11）返回完整 `Track` 格式（含字符串时间戳，前端兼容旧的 fromBackendTrack 转换器）。两种格式并存，导入路径走 `fromTrackDto()`（零时间戳解析），DB 加载路径走 `fromBackendTrack()`（含 parseTimestamp）。

## 六、组件树与数据流

```
App.vue (根组件，状态编排中心)
├── CesiumMap.vue (核心渲染，~740行)
│   └── watches: tracks / replayTime / lineWidths / visibility / showLabels / selectedId / flags / selectedPair
├── LayerControl.vue (图层显隐开关，per-source 切换)
├── FlagPanel.vue (旗标管理：增删改查 + 配对测距)
├── TimeFilterPanel.vue (起止时间选择 + 点数量阈值)
├── TrackPanel.vue (航迹列表 + 搜索 + 详情展开)
└── PlaybackBar.vue (播放/暂停/变速/进度拖拽)
```

**核心数据流**：

```
文件导入
  → useTrackLoader (Rust IPC + 分块转换 300 条/批)
    → useTracks().addTracks() (去重合并 + 位置点排序)
      → useTrackFilter (时间窗口 + 点数量阈值)
        → displayTracks (computed)
          ├─→ CesiumMap (3D 渲染：suspendEvents/resumeEvents 批量更新)
          ├─→ TrackPanel (列表展示)
          └─→ useReplay (二分查找 + 线性插值)
              → replayTime → CesiumMap (Billboard 位置更新)
```

## 七、启动流程

```
main.rs → run()
  → tauri::Builder::default()
    → .plugin(tauri_plugin_opener)       // 注册 opener 插件
    → .plugin(tauri_plugin_dialog)       // 注册 dialog 插件
    → .setup(|app| {
        ① resource_dir → find_mbtiles() 扫描 .mbtiles 文件
        ② start_tile_server(mbtiles_path) 启动内嵌 HTTP 瓦片服务
        ③ app_data_dir → db::db_path() 解析数据库路径
        ④ db::init_db(&db_file):
           a. PRAGMA journal_mode=WAL           // 启用以支持大事务
           b. CREATE TABLE IF NOT EXISTS        // 4 张核心表
           c. ALTER TABLE 增量迁移               // has_column() 检测 + PRAGMA table_info
           d. backfill_metadata()               // 回填 source/min/max/point_count 元数据
           e. PRAGMA user_version 检查          // O(1) 版本检测
           f. backfill_track_points()           // 分块回填位置点（仅 v0→v1）
           g. CREATE INDEX IF NOT EXISTS        // 8 个索引
           h. PRAGMA user_version = 1           // 标记迁移完成
        ⑤ app.manage(DbPath(Mutex::new(db_file))) 注入 Tauri 管理状态
      })
    → .invoke_handler(generate_handler![11 个命令])
    → .run()
```

前端 `onMounted`：
```
① invoke('get_tile_server_port') 获取瓦片端口
② new Cesium.Viewer() + UrlTemplateImageryProvider 创建 3D 场景
③ invoke('load_persisted_tracks') 加载全部历史航迹 → setAll(fromBackendTracks())
④ invoke('get_batches_cmd') 刷新批次列表
⑤ syncEntities(tracks) 初次渲染
⑥ pinIconDataUrl = createPinIcon() 生成旗标 canvas 图标
⑦ syncFlagEntities() 同步旗标实体
⑧ 注册交互处理器：LEFT_CLICK / MOUSE_MOVE / LEFT_DOUBLE_CLICK
```

## 八、交叉验证方法

1. **叠加对比**：ADS-B 真实航迹（青） + 雷达仿真航迹（绿）同场景叠加，直观评估偏离程度
2. **逐帧回放**：统一时间轴同步回放，观察各时刻空间位置差异
3. **时间窗口**：聚焦关键航段（起飞/巡航/着陆），排除无关时段干扰
4. **Vincenty 测距**：旗标配对测距，量化任意两点的椭球面距离偏差（< 0.5mm 精度）
5. **航迹隔离**：点击航迹进入隔离视图，排除其他航迹视觉干扰，专注分析单条轨迹

## 九、已知待优化项

1. **CesiumMap.vue 拆分**：740 行中包含 5+ 职责（实体管理/交互处理/旗标渲染/弧线绘制/style 计算），可拆分为多个 composable（useTrackEntities / useInteraction / useFlagRenderer）和子组件
2. **瓦片服务连接复用**：每个请求 `Connection::open()` SQLite，高频访问时建议使用 r2d2-sqlite 连接池或线程局部连接
3. **二分查找重复实现**：`useReplay.ts` 和 `CesiumMap.vue` 中各有独立的二分查找 + 插值逻辑，应抽取为共享的 `binarySearchPosition` 工具函数
4. **拖拽导入 onDrop 未实现**：目前仅显示了拖拽提示 overlay，实际文件解析逻辑未挂接（需 Tauri 的拖拽事件支持）
5. **SQL 窗口函数航迹匹配**：`track_points` 表已就绪（28 万行，含 icao_address + timestamp_ms + lat/lng），可利用 SQLite 的 LAG/LEAD/PARTITION BY 实现时间-空间维度的航迹关联匹配，是下一步的核心优化方向

## 十、项目目录结构

```
RadarView_BiuldByTauri/
├── src/                              # Vue 3 前端
│   ├── App.vue                       # 根组件（面板编排 + IPC 调用，~500行）
│   ├── main.ts                       # Vue 应用入口
│   ├── types/
│   │   └── track.ts                  # Track/TrackPoint/DataSource 类型定义
│   ├── components/
│   │   ├── CesiumMap.vue             # 3D 渲染核心（~740行）
│   │   ├── TrackPanel.vue            # 航迹列表面板（搜索 + 详情）
│   │   ├── PlaybackBar.vue           # 回放控制条
│   │   ├── LayerControl.vue          # 图层显隐控制
│   │   ├── FlagPanel.vue             # 旗标管理面板
│   │   └── TimeFilterPanel.vue       # 时间窗口过滤面板
│   └── composables/
│       ├── useTracks.ts              # 航迹状态管理（CRUD + 合并去重）
│       ├── useTrackFilter.ts         # 时间窗口 + 按源点数量过滤
│       ├── useReplay.ts              # 回放引擎（二分插值 + RAF 驱动）
│       ├── useFlags.ts               # 旗标系统（增删改 + Vincenty 测距）
│       ├── useLayerVisibility.ts     # 图层显隐（reactive 单例）
│       ├── useLabelVisibility.ts     # 标签开关
│       ├── useLineWidth.ts           # 线宽调节（reactive 单例，0.5-10px）
│       ├── useTrackStyle.ts          # 数据源 → 颜色/图标映射
│       ├── useTrackLoader.ts         # 文件导入流程 + 分块进度
│       ├── useGeoCalc.ts             # 大地测量计算（纯函数）
│       └── convertTrack.ts           # Rust snake_case → TS camelCase
├── src-tauri/                        # Rust 后端
│   ├── src/
│   │   ├── main.rs                   # 程序入口
│   │   ├── lib.rs                    # Tauri 命令注册 + setup 钩子
│   │   ├── track.rs                  # Track/TrackPosition 数据模型
│   │   ├── adsb.rs                   # ADS-B CSV 解析器
│   │   ├── radar.rs                  # MAT 文件解析 + 子进程调度
│   │   ├── db.rs                     # 数据库核心（~620行，含 WAL/回填/迁移/查询）
│   │   └── tile_server.rs            # 内嵌 HTTP 瓦片服务（XYZ→TMS→SQLite）
│   ├── resources/
│   │   ├── convert_mat.exe           # PyInstaller 打包的 MAT 转换器
│   │   └── *.mbtiles                 # 离线瓦片文件（1.1GB）
│   └── tauri.conf.json               # Tauri 配置（窗口/安全/打包）
├── radarview-web/                    # Web 版本（独立前端）
├── package.json                      # 前端依赖
├── vite.config.ts                    # Vite 构建配置
└── tsconfig.json                     # TypeScript 配置
```

## 十一、关键问题与解决方案汇总

| 问题 | 现象 | 根因 | 解决方案 |
|------|------|------|----------|
| 数据库 B-tree 损坏 | integrity_check 报 rowid out of order | delete mode 下 28 万行单事务 INSERT 导致页分裂异常 | WAL 模式 + Rust 分块事务（50 航迹/trx，500 行/INSERT）+ PRAGMA user_version |
| 线宽滑块无效果 | 拖动滑块后折线宽度不变 | CesiumMap 缺少对 props.lineWidths 的 watch 监听 | 添加 `watch(() => props.lineWidths, ..., { deep: true })` 遍历更新 polyline.width |
| 幽灵航迹 | ADS-B CSV 中产生空 ICAO 地址的无效航迹 | 原解析器未过滤空行 | 在分组阶段过滤 icao_address.is_empty() |
| 时区偏移 | datetime-local 输入时间与实际过滤不符 | 浏览器本地时区与 UTC 转换不一致 | 统一使用 epoch 毫秒作为内部时间表示 |
| 多次过滤航迹消失 | 第二次过滤后航迹丢失 | 过滤后的 positions 覆盖了原始数据 | 使用 `{ ...track, positions: filtered }` 浅拷贝而非原地修改 |
| 导入耗时 15s | 用户点击导入后长时间无反馈 | 串行链路：解析→DB6-8s→IPC 40MB→前端转换，全同步阻塞 | 异步持久化 + 紧凑 TrackDto IPC（12MB）+ rayon 并行解析 + 字节算术 ts_to_ms，降至 5-6s |
| DTO 转换成瓶颈 | Rust 侧 ts_to_ms 耗时 1.34s（比 JS 的 charCode 还慢） | chrono 的 NaiveDateTime::parse_from_str 做 format-string 解析极重 | 改用字节索引算术 + NaiveDate/NaiveTime 构造器，276K 次调用从 1.34s → 50ms |
| IPC 传输慢 | 40MB JSON 在 Tauri webview channel 中传输 2-3s | 字段名冗长 + 时间戳字符串占 19 字节 + 对象序列化开销 | TrackDto 短字段 + PtDto tuple 序列化 + epoch ms 整型时间戳，JSON 缩减至 12MB |
| raylex 类型推断失败 | par_iter().fold() 的 BTreeMap::new() 无法推断 V 类型参数 | rayon fold 闭包的匿名类型推断限制 | 使用类型别名 GroupMap / MetaMap 显式标注 fold 和 reduce 的初始值 |

## 十二、技术亮点总结

1. **数据库架构演进**：从 JSON Blob 单一存储演进为双表（Blob + 行式位置点），在不破坏现有功能的前提下为 SQL 窗口函数航迹匹配打下基础
2. **生产级数据迁移**：WAL 模式 + 分块事务 + INSERT OR IGNORE + PRAGMA user_version + 延迟建索引，五重保障 28 万行历史数据安全回填
3. **Vue 3 深度响应式**：`reactive` + `watch({ deep: true })` 实现滑块拖动时 Cesium 折线的零延迟更新，无需重建渲染实体
4. **批量渲染优化**：`suspendEvents/resumeEvents` 将 N 次渲染合并为 1 次，requestRenderMode 按需渲染最小化 GPU 消耗
5. **二分插值回放**：O(log N) 时间复杂度 + 6 维字段线性插值，支持 2000x 极速回放不丢帧
6. **模块级单例状态管理**：11 个 composable 通过 ES Module 天然单例实现跨组件共享，零依赖、零 boilerplate
7. **离线优先架构**：1.1GB MBTiles 瓦片 + 内嵌 HTTP 服务器，完全脱离互联网运行
8. **防御性 UI 设计**：数据库删除二次确认、并发操作互斥锁、导入进度反馈、错误 toast 提示
9. **异步持久化架构**：从串行"解析→存库→返回"重构为"解析→立即返回渲染 + 后台线程写库"，用户感知等待从 15s → 5s
10. **紧凑 IPC 协议**：TrackDto/PtDto 短字段名 + tuple 数组序列化 + epoch ms 整型时间戳，JSON 体量从 40MB 降至 12MB（70% 压缩率），IPC 传输 + 前端转换从 ~4s → ~1.5s
11. **C 风格字节时间戳解析**：`ts_to_ms()` 放弃 chrono 的 format-string 解析，改为 as_bytes 索引算术 + NaiveDate/NaiveTime 构造器，276K 次调用从 1.34s → 50ms（26x 加速）
12. **rayon 多线程并行**：CSV 解析两阶段并行（fold+reduce → map），DTO 转换并行（into_par_iter），充分利用 4 核 CPU，CSV 解析 2.0s → 1.4s
13. **可观测性能剖析**：Rust stderr + 前端 console.log 双层 `[perf]` 计时日志，精确定位 IPC call / CSV parse / DTO convert / Cesium sync 等的分段耗时

