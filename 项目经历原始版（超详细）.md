# RadarView — 雷达航迹可视化验证平台

> 项目完整技术文档 | 哈尔滨工业大学 电子信息专业 大三 | 2025年4月 — 2025年8月

## 一、项目背景

课题组与中国电子科技集团第二十二研究所合作雷达航迹形成项目。用 MATLAB 对雷达原始回波进行信号检测、点迹凝聚、航迹起始、滤波跟踪形成仿真航迹，需将仿真航迹与真实 ADS-B 数据在 3D 场景叠加对比以验证算法准确性。

痛点：MATLAB 只能 2D 展示，无法多数据源叠加、无时间轴回放、MAT/CSV 格式不统一。
成果：开发了 Tauri + Vue 3 + CesiumJS 桌面端可视化平台，已投入课题组日常使用。

## 二、量化指标

| 指标 | 数值 |
|------|------|
| 总代码量 | ~4000 行（前端 3239 行 + Rust 726 行） |
| 源文件数 | 27 个（20 前端 + 7 Rust） |
| 前端组件 | 6 个 Vue 组件 |
| Rust 函数 | 32 个 |
| 数据库表 | 2 张 |
| 前端打包体积 | 104KB |
| 可执行文件 | 17MB |
| 离线瓦片 | 1.1GB Natural Earth（0-8级） |
| CSV 测试数据 | 29MB / 27.8万行 / 2901条航迹 |
| MAT 测试数据 | 8.7MB |
| 回放速度 | 100x - 2000x（可自定义） |

## 三、技术架构

### 3.1 整体架构

三层架构：Vue 3 前端（CesiumJS 渲染层 + Composables 状态层）通过 Tauri IPC（JSON序列化）与 Rust 后端（文件解析/数据库/内嵌HTTP瓦片服务）通信。

### 3.2 技术栈

| 层级 | 技术 |
|------|------|
| 桌面框架 | Tauri 2（Rust后端+系统WebView） |
| 前端 | Vue 3 Composition API + TypeScript |
| 3D引擎 | CesiumJS 1.140 (WGS-84) |
| 数据库 | SQLite (rusqlite) |
| 瓦片服务 | tiny_http (Rust内嵌HTTP) |
| 文件对话框 | @tauri-apps/plugin-dialog |
| 外部工具 | convert_mat.exe (MAT文件转换) |
| 地图数据 | Natural Earth MBTiles |

### 3.3 前端状态管理（Composables单例模式）

不使用Vuex/Pinia。10个composable模块在文件顶层声明ref()实现跨组件共享：

| Composable | 职责 |
|------------|------|
| useTracks | 航迹CRUD、选择、隔离 |
| useTrackFilter | 时间范围过滤 |
| useReplay | 回放引擎（播放/暂停/调速/插值） |
| useFlags | 旗标标注系统（放置/重命名/删除/测距） |
| useLayerVisibility | 图层显隐控制（per-source） |
| useLabelVisibility | 航迹标签开关 |
| useTrackStyle | 数据源到颜色映射（纯函数） |
| convertTrack | Rust->TS 格式转换（纯函数） |
| useGeoCalc | 大地测量计算（纯函数） |
| useTrackLoader | 文件导入流程编排+进度 |

## 四、核心功能详解

### 4.1 多数据源导入管线

**ADS-B CSV导入流程**：
1. plugin-dialog打开原生文件选择框 -> invoke发送路径到Rust
2. adsb::parse_adsb_csv()逐行解析19字段CSV，按ICAO地址分组到BTreeMap
3. 每组内按时间戳排序，首行提取元数据（航班号/机型/注册号）
4. db::save_batch()持久化到SQLite（含文件名去重检查）
5. 返回Vec<Track> JSON -> convertTrack.ts转换snake_case为camelCase
6. 时间戳字符串(charCodeAt快速解析)转为epoch毫秒，英尺转米(x0.3048)
7. 分块转换：每300条await nextTick()让出UI线程
8. useTracks().addTracks()合并 -> CesiumMap watch自动同步渲染

**CSV 19列格式**：0=ICAO地址 1=纬度 2=经度 3=航向 4=高度(英尺) 5=地速 8=机型 9=注册号 10=时间戳 11=起飞机场 12=目的机场 13=航班号 15=垂直速率 16=ICAO航班号 18=航司

**雷达MAT导入流程**：
1. radar::parse_mat_file() -> find_converter()三阶段搜索convert_mat.exe
2. 启动子进程（带--mode raw为原始测量模式），120秒超时保护（跨平台kill）
3. Tauri Event推送进度：converting(10%) -> parsing(80%) -> done(90%)
4. stdout JSON -> serde反序列化 -> source覆盖（标准Radar/原始RadarRaw）

### 4.2 数据库设计

```sql
CREATE TABLE batches (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    file_name TEXT NOT NULL, source TEXT NOT NULL,
    track_count INTEGER NOT NULL, imported_at TEXT NOT NULL
);
CREATE TABLE saved_tracks (
    icao_address TEXT NOT NULL, batch_id INTEGER NOT NULL,
    track_json TEXT NOT NULL,
    PRIMARY KEY (icao_address, batch_id),
    FOREIGN KEY (batch_id) REFERENCES batches(id) ON DELETE CASCADE
);
```

设计决策：track_json存储整条航迹JSON blob而非拆分位置点表——避免多表JOIN和N+1查询，航迹总是以整体被查询。
去重：batch_exists()按file_name查询。并发安全：DbPath包装在Mutex<PathBuf>中作为Tauri Managed State。
批次管理4个API：load_persisted_tracks / get_batches_cmd / load_batch_tracks_cmd / delete_batch_cmd

### 4.3 3D地图渲染（CesiumJS）

初始化：invoke获取瓦片端口 -> Cesium.Viewer（scene3DOnly/requestRenderMode/关闭天空大气/baseLayer:false）-> UrlTemplateImageryProvider(0-8级) -> 相机初始位置(110E,25N,12000km)

航迹实体：每条航迹创建Polyline(飞行路径)+Billboard(当前位置点)。Entity ID：{trackId}::line和{trackId}::dot。

颜色方案：ADS-B=青色#00d4ff(line2.0/alpha0.88) Radar=绿色#00ff88 RadarRaw=橙色#ff8800(line1.0/alpha0.6)

同步策略：watch props.tracks -> suspendEvents/resumeEvents批量更新（避免逐个触发事件）。

高亮交互：选中=线宽4.0/alpha1.0/billboard1.2x 悬停=红色#ff3333/线宽5.0 点击=LEFT_CLICK pick实体emit track-pick

双击：禁用默认双击缩放。双击旗标=删除，双击空白=pickEllipsoid获取经纬度放置新旗标。

### 4.4 时序回放引擎

二分查找O(log n)+线性插值。对每条航迹：确定currentTime落在哪两个点之间 -> 计算t=(t-lo)/(hi-lo) -> 对所有字段(经纬度/高度/航向/地速/垂直速率)线性插值。

requestAnimationFrame驱动：wallDelta x speed -> currentTime += dataDelta -> 到末尾自动暂停 -> 更新所有billboard位置。
变速：100x/300x/500x/2000x四档+自定义。航迹变更时watch自动暂停并重置。

### 4.5 时间窗口过滤

setUniversalTimeRange(min,max)：筛选timestamp在区间内的位置点，清空位置数为0的航迹。clearAllTimeRanges()恢复。filteredTracks为computed属性。

### 4.6 旗标标注与大地测量

旗标CRUD：双击空白放置(自动命名旗标N) / 手动输入坐标 / 点击标签重命名 / X按钮或双击图标删除 / checkbox选2个用于测距

大地测量算法：haversineKm(球面~0.3%误差) vincentyKm(WGS-84椭球面<0.5mm精度 100次迭代 对跖点兜底) initialBearing(大圆方位角) bearingToCardinal(8方位映射)

大圆弧可视化：Cesium.EllipsoidGeodesic计算64中间点渲染黄色polyline

### 4.7 离线瓦片服务器

tile_server.rs：启动时find_mbtiles()扫描.mbtiles -> start_tile_server()绑定127.0.0.1:0 -> 后台线程tiny_http处理请求 -> 解析/tiles/{z}/{x}/{y}.png -> XYZ转TMS: tms_y=(1<<z)-1-y -> SQLite查询tile_data -> 返回PNG+CORS头 -> get_tile_server_port()暴露端口。

已知缺陷：每个请求都Connection::open()SQLite，应改为复用连接。

## 五、Tauri IPC接口清单

共9个命令（lib.rs generate_handler!）：

| # | 命令 | 参数 | 返回 |
|---|------|------|------|
| 1 | greet | name | String |
| 2 | get_tile_server_port | - | u16 |
| 3 | import_adsb_file | file_path | Result<Vec<Track>> |
| 4 | import_radar_file | file_path | Result<Vec<Track>> |
| 5 | import_radar_raw_file | file_path | Result<Vec<Track>> |
| 6 | load_persisted_tracks | - | Result<Vec<Track>> |
| 7 | get_batches_cmd | - | Result<Vec<BatchInfo>> |
| 8 | load_batch_tracks_cmd | batch_id | Result<Vec<Track>> |
| 9 | delete_batch_cmd | batch_id | Result<()> |

所有命令返回Result<T,String>，前端try/catch处理并显示错误。

## 六、组件树与数据流

```
App.vue
+-- CesiumMap.vue (716行核心)  +-- LayerControl.vue
+-- FlagPanel.vue              +-- TimeFilterPanel.vue
+-- TrackPanel.vue             +-- PlaybackBar.vue
```

数据流：useTracks -> useTrackFilter -> displayTracks(computed) -> useReplay+CesiumMap+TrackPanel

## 七、启动流程

main.rs->run() -> 注册插件 -> setup(){找MBTiles/启动瓦片/初始化SQLite/注入DbPath} -> 开窗口 -> 前端onMounted{获取瓦片端口/创建Viewer/加载历史航迹/渲染批次}

## 八、交叉验证方法

叠加对比(ADS-B+仿真同场景)、逐帧回放(时间轴同步)、时间窗口(聚焦关键航段)、Vincenty测距(量化偏差)、航迹隔离(排除干扰)

## 九、已知待优化项

1. CesiumMap.vue拆分（5+职责混在716行）
2. 瓦片服务连接复用
3. 二分查找重复实现
4. 数据源颜色4处硬编码
5. 拖拽导入onDrop未实现

## 十、项目目录结构

```
src/ (Vue3前端: 6组件+10composables)
src-tauri/ (Rust后端: 7源文件)
  src/main.rs src/lib.rs src/track.rs src/adsb.rs src/radar.rs src/db.rs src/tile_server.rs
  natural_earth.mbtiles (1.1GB)
package.json vite.config.ts tsconfig.json
```
