server:
  port: 8084

spring:
  application:
    name: radarview-tile
  data:
    redis:
      host: localhost
      port: 6379
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

tile:
  mbtiles-path: /data/tiles/tianditu.mbtiles
  cache-ttl-hours: 24
