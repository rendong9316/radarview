# Loop Budget — RadarView

- Max tokens/day: 500k (adjust per user directive)
- On exceed: pause sub-agent spawns, notify human
- Max sub-agent spawns per run: 2
- Kill switch: `loop-pause-all` — pause schedulers and notify human
